# ------------------------------------------------------------------
# health can't be more than 200
#
# @Caption("D3");
# ------------------------------------------------------------------

G (∀x ∈ /message/player/actualWeapon : (∀y ∈ /message/player/weapons[//owned=$x]//ammos : (¬(y > 200))))
