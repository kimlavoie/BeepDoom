# ------------------------------------------------------------------
# shells can't be more than 50
#
# @Caption("D4");
# ------------------------------------------------------------------

 G(∀x ∈ /message/player/weapons/weapon[name="shotgun"]/ammos : ((¬(x > 50)) ∧ (x > 0)))
